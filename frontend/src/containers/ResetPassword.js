import React from 'react';
import Layout from "../components/Layout";

function ResetPassword() {
    return (
        <Layout title="Todo | Reset Password" content="Reset Password Page">
            <h1>Reset Password</h1>
        </Layout>
    );
}

export default ResetPassword;